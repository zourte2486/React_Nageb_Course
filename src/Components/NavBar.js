export default function NavBar() {
  return (
    <div>
      <img
        className="image"
        src={require("../Img/React.webp")}
        style={{ display: "flex", justifyContent: "center", width: "200px" }}
        alt="logo"
      />
    </div>
  );
}
